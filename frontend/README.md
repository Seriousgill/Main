# Ticket Booking Frontend (Next.js)

## Pages
- `/` event grid listing
- `/events/[id]` event details
- `/checkout/[bookingId]` payment initiation
- `/dashboard` user bookings

## Setup
1. Set `NEXT_PUBLIC_API_BASE=http://localhost:8000/api`
2. Install dependencies and run `npm run dev`

